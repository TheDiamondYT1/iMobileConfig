<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
	<PreferenceCategory
		android:title="WebClips">
		
        <ListPreference
			android:key="webClipUserAgent"
            android:defaultValue="default"
            android:entries="@array/webClipUAEntries"
            android:entryValues="@array/webClipUAEntriesValues"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"
            android:title="Select WebClip User Agent" />
	</PreferenceCategory>
</PreferenceScreen>
